name : CD Production

on:
    push:
        branches: [ main ]
jobs:
    build:
        runs-on: self-hosted
        steps:
            - uses: actions/checkout@v2
            - name: Build Container
              run: docker-compose --file docker-compose.yml up -d --build ggfs